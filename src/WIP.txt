ARX:
    ARXAudioEncoder output formatting
    ARXAudioEncoder MQ: which one?
    project-keyword-spotter integration? feasible, alt. solution
    Elasticsearch mappings
    data persistence?

BLU:
    NOT STARTED

CAM:
    automate FrameObjektTracker output analysis.
    start encoding frames using FrameObjektEncoder
    FrameObjektEncoder Elasticsearch mappings

CAN:
    NOT STARTED

CONFIG:
    move config into modules?

EBS:
    COMPLETED FOR NOW.

GPS:
    GPS via hardware
    implement GPS via JS

LIB:

MAP:
    mapaggregator refresh; read from rabbitMQ, not REST
    hyperspectral imaging: BLOCKED DUE TO ENVIRONMENT
    Elastic Integration: can I pull() data from Elastic and put it on the existing map component?

MOT:
    NEVER STARTED; may go away....

NET:
    FrameObjektEncoder: imageZMQ
    ARXEncoder: zeromq
    WifiSignalPoint Elastic mappings. Dense vectors, nested data

PSX:
    GPT: NOT STARTED. Use Pyshark

SDR:
    Capture signals to discrete arrays
    SignalPoint processing via MQ! not a local method. offline proccessing...
    ARXAudioEncoder via MQ! not a local method. offline proccessing...
    Elasticsearch mappings

TRX:
    TRXUSBRetriever docker solution cont'd
    produce internal maps over rabbitMQ
    TRXSerialRetriever may be kaput on Mac?
    TRXSignalPoint Elasticsearch mappings

VIEW:
    Integrate Kibana: blocker is elastic https
    Integrate SDR: jupyterlite w/ matplotlib inline + custom javascript vertical waterfall served via node.

WIFI:
    refactor retrieval out, and produce internal maps over rabbitMQ
    refactor to read ALL SignalPoint types from MQ

DOCKER:
    compose.yaml
    Elasticsearch:
        https for Kibana integration
    Kibana:
        dashboards (read serialized json output on filesystem)

    Rabbit MQ startup

    node processes.


